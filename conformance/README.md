# Conformance

Every AuditSpec implementation should validate all files under `valid/` and reject all files under `invalid/`.

Future versions will add behavioral tests for redaction, delegation, ordering, atomicity adapters, and CloudEvents mappings.

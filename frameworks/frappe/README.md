# Frappe / ERPNext integration

Frappe's Version and Access Log features should remain the native low-level history mechanisms. AuditSpec complements them with semantic business, security, and agent actions.

Example categories include `estimate.approve`, `member.role_change`, `agent.project_update`, and `export.denied`.
